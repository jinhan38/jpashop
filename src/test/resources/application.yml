# jdbc:h2:mem:test -> 서버를 메모리 모드로 실행시켜 준다.
# 그런데 스프링부트는 별도의 설정 없으면 메모리 모드로 실행시킨다.
# 그래서 주석처리 해도 된다.
spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace
